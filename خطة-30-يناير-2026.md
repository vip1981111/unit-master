# خطة 30 يناير 2026 - إعداد التحكم بالمشروع من الجوال

## 📋 ملخص المشروع

تم إعداد نظام يسمح بالتحكم في مشروع Flutter من الجوال عبر GitHub Issues.
الـ Mac في البيت يعمل كـ server خاص ومجاني.

---

## ✅ ما تم إنجازه

### المرحلة 1: إعداد الـ Mac

#### 1.1 التأكد من Flutter
```bash
flutter doctor
# النتيجة: Flutter 3.38.4 ✅
```

#### 1.2 التأكد من Xcode
```bash
xcode-select --install
# Xcode 26.1 ✅
```

#### 1.3 إنشاء مشروع Flutter جديد
```bash
cd ~
flutter create mobile_control_app
cd mobile_control_app
```

#### 1.4 تجربة iOS Simulator
```bash
open -a Simulator
flutter run
# التطبيق يعمل ✅
```

#### 1.5 إعدادات الطاقة
```
System Settings → Battery → Options:
  ✅ منع الإسبات التلقائي أثناء التشغيل على محول الطاقة عند توقف الشاشة
  ✅ تنبيه عند الاتصال بالشبكة: دائماً
```

#### 1.6 تثبيت AlDente لحماية البطارية
```bash
brew install --cask aldente
```
- Charge Limit: 80%
- Launch at Login: ✅

---

### المرحلة 2: إعداد GitHub

#### 2.1 رفع المشروع على GitHub
```bash
cd ~/mobile_control_app
git init
git add .
git commit -m "Initial Flutter project"
git remote add origin https://github.com/vip1981111/mobile-control-app.git
git branch -M main
git push -u origin main
```

#### 2.2 إنشاء Personal Access Token
```
https://github.com/settings/tokens/new
- Note: mobile-control-mac
- Expiration: No expiration
- Scopes: ✅ repo, ✅ workflow
```

#### 2.3 إضافة ملف GitHub Actions
```bash
mkdir -p .github/workflows
```

ملف `.github/workflows/claude-mobile.yml`:
```yaml
name: Claude Mobile Control

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  claude-response:
    if: contains(github.event.comment.body || github.event.issue.body, '@claude')
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter Path
        run: echo "$HOME/development/flutter/bin" >> $GITHUB_PATH

      - name: Check Flutter
        run: flutter --version

      - name: Install Dependencies
        run: flutter pub get

      - name: Run Tests
        run: flutter test

      - name: Comment Result
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ تم تشغيل الاختبارات بنجاح!'
            })
```

#### 2.4 إضافة Anthropic API Key
```
https://github.com/vip1981111/mobile-control-app/settings/secrets/actions/new
- Name: ANTHROPIC_API_KEY
- Secret: sk-ant-...
```

#### 2.5 تثبيت GitHub Runner
```bash
cd ~
mkdir actions-runner && cd actions-runner
curl -o actions-runner-osx-arm64-2.331.0.tar.gz -L https://github.com/actions/runner/releases/download/v2.331.0/actions-runner-osx-arm64-2.331.0.tar.gz
tar xzf ./actions-runner-osx-arm64-2.331.0.tar.gz
./config.sh --url https://github.com/vip1981111/mobile-control-app --token [TOKEN]
```

#### 2.6 تشغيل Runner كـ Service
```bash
./svc.sh install
./svc.sh start
./svc.sh status
# Status: Started ✅
```

---

### المرحلة 3: التجربة

1. فتح GitHub من الجوال
2. إنشاء Issue جديد أو Comment
3. كتابة: `@claude test`
4. النتيجة: ✅ تم تشغيل الاختبارات بنجاح!

---

## 📱 كيفية الاستخدام

### من الجوال:
1. افتح تطبيق GitHub أو المتصفح
2. اذهب إلى: `github.com/vip1981111/mobile-control-app/issues`
3. أنشئ Issue جديد أو أضف Comment
4. اكتب: `@claude [الأمر]`

### الأوامر المتاحة:
| الأمر | الوظيفة |
|-------|---------|
| `@claude test` | تشغيل الاختبارات |
| `@claude build` | بناء التطبيق |
| `@claude analyze` | تحليل الكود |

---

## 🔗 الروابط المهمة

- **المشروع:** https://github.com/vip1981111/mobile-control-app
- **Actions:** https://github.com/vip1981111/mobile-control-app/actions
- **Issues:** https://github.com/vip1981111/mobile-control-app/issues
- **Secrets:** https://github.com/vip1981111/mobile-control-app/settings/secrets/actions

---

## ⚙️ أوامر صيانة الـ Runner

```bash
# التحقق من حالة الـ Runner
cd ~/actions-runner
./svc.sh status

# إعادة تشغيل الـ Runner
./svc.sh stop
./svc.sh start

# إلغاء تثبيت الـ Runner
./svc.sh uninstall
```

---

## 🔋 إدارة AlDente

- التطبيق يعمل تلقائياً عند تشغيل الـ Mac
- البطارية محدودة عند 80%
- يمكن تعديل الإعدادات من أيقونة AlDente في شريط القوائم

---

## 📅 التاريخ

- **تاريخ الإعداد:** 30 يناير 2026
- **المستخدم:** Mohammed Abdullah
- **الجهاز:** MacBook Pro (Apple Silicon - ARM64)
